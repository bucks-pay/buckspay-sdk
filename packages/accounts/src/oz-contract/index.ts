/**
 * OpenZeppelin Smart Account adapter for `C…` contract accounts + passkey
 * (`@buckspay/accounts/oz-contract`). Implemented in Sprint 4/04 — placeholder
 * kept so the subpath export resolves.
 */
export const version = "0.0.0";
