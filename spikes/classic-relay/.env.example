# Facilitator (the SDK's relayer target). Spike is a Node script → key allowed here, NEVER in client code.
FACILITATOR_URL=https://your-facilitator.example
FACILITATOR_API_KEY=replace-with-server-side-key
# Stellar testnet endpoints
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
HORIZON_URL=https://horizon-testnet.stellar.org
# USDC SAC contract id on testnet (matches facilitator STELLAR_USDC_TESTNET_SAC)
USDC_SAC=CBIELTK6YBZJU5UP2WWQEUCYKLPU6AUNZ2BQ4WWFEIE3USCIHMXQDAMA
# A FUNDED testnet ed25519 secret (S...). Stand-in for the browser wallet. Node-only, never in any SDK package.
PAYER_SECRET=Sxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# A funded testnet G-address to receive the transfer (must exist with a USDC trustline).
RECIPIENT_PUBLIC_KEY=Gxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
